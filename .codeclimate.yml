version: "2"
plugins:
  duplication:
    enabled: true
    config:
      languages:
      - ruby
  fixme:
    enabled: true
  rubocop:
    enabled: true
    channel: rubocop-0-55
    config:
      file: .rubocop.yml
exclude_patterns:
- "spec/"
- "test/"
