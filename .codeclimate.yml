version: "2"
engines:
  duplication:
    enabled: true
    config:
      languages:
      - ruby
plugins:
  fixme:
    enabled: true
  foodcritic:
    enabled: true
  rubocop:
    enabled: true
ratings:
  paths:
  - "**.rb"
exclude_paths:
- spec/
- test/
